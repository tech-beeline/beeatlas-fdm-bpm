spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fdmbpm
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: camunda
